import { deleteItem } from '../../src/utils/Array';

describe('utils/Array.ts', () => {
  it('', () => {
    // Given

    // When

    // Then
    console.log(deleteItem);
    expect(1).toBe(1);
  });
});
